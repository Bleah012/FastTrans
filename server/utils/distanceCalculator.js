const calculateDistance = (source, destination) => {
  // This mock can later be replaced by Google Maps API or another distance API.
  if (!source || !destination) {
    return 15;
  }

  return 15;
};

module.exports = {
  calculateDistance,
};
